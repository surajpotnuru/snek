version: 0.1.0

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install
  pre_build:
    commands:
      - echo "In pre build phase"
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo "In post build phase"